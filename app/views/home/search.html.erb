<p>Search result(s) with "<%= params["name"]%>"</p>
<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
  </tr>

<% @user.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
  </tr>
<% end %>
</table>
<p><%= link_to('back', root_path) %></p>

